# The 647 channel actually observed in generations A/B, transcribed as is.
#
# The purpose of this file is not to make it pass but to **show the gate rejecting it**.
# The information left in the metadata alone cannot render a verdict on optical validity.
#
#   python -m optics.cli check config/channels/legacy-observed.yaml
#
# Expected result: BLOCKED
#   - passband of the 'DA/FI/TR10Empty' cube unknown
#   - filter at position 'Wheel-A:Filter-0' unknown
#   - objective NA unverified
#
# Until these three are filled in, no recommendation has any basis.

channel:
  name: "647-Cy5 (as observed)"
  dye: SA647                      # ⚠ actual fluorophore unsettled
  objective:
    label: "5-Plan Apo Lmbd 100x Oil"
    magnification: 100
    na: 1.45                      # ⚠ assumed value
    immersion: oil
    verified_na: false
  detector: Prime95B
  source: [Spectra, Red]          # observed as Spectra-Red_Level: 10
  excitation: []                  # no excitation filter on record (assumed in-cube)
  dichroic: "DA/FI/TR10Empty"     # kind: unknown -> BLOCKED
  emission: ["Wheel-A:Filter-0"]  # kind: unknown -> BLOCKED
  port_fraction: 1.0              # LightPath 4-L100
